{
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix client-dashboard-vite",
    "build": "npm run build --prefix client-dashboard-vite",
    "build:vercel": "npm run build:vercel --prefix client-dashboard-vite",
    "preview": "npm run preview --prefix client-dashboard-vite",
    "deploy": "npm run deploy:prepare --prefix client-dashboard-vite",
    "deploy:prod": "npm run build:vercel --prefix client-dashboard-vite && npx vercel --prod",
    "lint": "npm run lint --prefix client-dashboard-vite",
    "typecheck": "npm run typecheck --prefix client-dashboard-vite",
    "clean": "npm run clean --prefix client-dashboard-vite",
    "perf:check": "npx lhci autorun"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "postcss": "^8.5.15",
    "tailwindcss": "^4.3.0",
    "vite": "^8.0.12"
  }
}
