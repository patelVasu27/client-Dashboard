{
  "private": true,
  "workspaces": [
    "client-dashboard-vite"
  ],
  "scripts": {
    "build": "cd client-dashboard-vite && npm run build && node -e \"const fs = require('fs'); fs.rmSync('../dist', { recursive: true, force: true }); fs.renameSync('dist', '../dist');\"",
    "build:vercel": "cd client-dashboard-vite && npm run build:vercel && node -e \"const fs = require('fs'); fs.rmSync('../dist', { recursive: true, force: true }); fs.renameSync('dist', '../dist');\"",
    "deploy:prepare": "cd client-dashboard-vite && npm run deploy:prepare"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "postcss": "^8.5.15",
    "tailwindcss": "^4.3.0"
  }
}
